{
  "name": "root",
  "private": true,
  "scripts": {
    "watch": "lerna run watch --parallel",
    "build": "lerna run build --stream",
    "lerna:version": "lerna version",
    "test": "lerna run test --scope @salable/stencil-library --scope react-client",
    "jest": "jest",
    "publish": "lerna publish --force-publish"
  },
  "devDependencies": {
    "@playwright/test": "^1.39.0",
    "@types/node": "^20.8.10",
    "lerna": "^7.4.2",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "stencil-playwright": "^0.3.0"
  }
}
